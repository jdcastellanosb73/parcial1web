
import Card from 'react-bootstrap/Card';
import {Link} from 'react-router-dom';


function Cafe(props){
   return(

<table class="table">
<thead>
  <tr>
    <th scope="col">#</th>
    <th scope="col">Nombre</th>
    <th scope="col">Tipo</th>
    <th scope="col">Region</th>
  </tr>
</thead>
<tbody>
  <tr>
    <th scope="row">1</th>
    while ({props.cafe.id}==1) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
  <tr>
    <th scope="row">2</th>
    while ({props.cafe.id}==2) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
  <tr>
    <th scope="row">3</th>
    while ({props.cafe.id}==3) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
  <tr>
    <th scope="row">4</th>
    while ({props.cafe.id}==4) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
  <tr>
    <th scope="row">5</th>
    while ({props.cafe.id}==5) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
  <tr>
    <th scope="row">6</th>
    while ({props.cafe.id}==6) {
         <><td>{props.cafe.id}</td><td>{props.cafe.nombre}</td><td>{props.cafe.tipo}</td><td>{props.cafe.region}</td></>
    }
  </tr>
</tbody>
</table>
   );
}
export default Cafe;